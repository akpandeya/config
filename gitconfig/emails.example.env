# Copy to emails.local.env (same directory) and fill in your real addresses.
# emails.local.env is gitignored — never commit real emails to this repo.
PERSONAL_EMAIL="you@example.com"
WORK_EMAIL="you@work.example.com"
