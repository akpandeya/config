[user]
    name = Avanindra Kumar Pandeya
    email = __PERSONAL_EMAIL__
    # Sign personal commits with the personal SSH key. GitHub verifies
    # the signature against the same public key it uses for push auth,
    # so no extra setup on github.com.
    signingkey = ~/.ssh/id_dipus_macbook

[core]
    sshCommand = ssh -o IdentitiesOnly=yes
